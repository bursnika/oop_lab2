<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="using:LibraryXmlProcessor.ViewModels"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="600"
        x:Class="LibraryXmlProcessor.Views.GoogleDriveDialog"
        x:DataType="vm:GoogleDriveDialogViewModel"
        Title="Google Drive - XML та HTML файли"
        Width="900" Height="650"
        WindowStartupLocation="CenterOwner"
        Background="#FFF5F7"
        CanResize="False">

    <Window.Styles>
        <Style Selector="Button">
            <Setter Property="FontFamily" Value="Segoe UI, Arial, sans-serif"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="15,10"/>
            <Setter Property="Margin" Value="5"/>
        </Style>

        <Style Selector="Button:pointerover">
            <Setter Property="Opacity" Value="0.85"/>
        </Style>

        <Style Selector="TextBlock">
            <Setter Property="FontFamily" Value="Segoe UI, Arial, sans-serif"/>
        </Style>

        <Style Selector="ListBox">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#FFD1DC"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="5"/>
        </Style>

        <Style Selector="ListBoxItem">
            <Setter Property="Padding" Value="15,12"/>
            <Setter Property="Margin" Value="5"/>
        </Style>

        <Style Selector="ListBoxItem:selected /template/ ContentPresenter">
            <Setter Property="Background" Value="#FFE5EC"/>
        </Style>

        <Style Selector="ListBoxItem:pointerover /template/ ContentPresenter">
            <Setter Property="Background" Value="#FFF0F5"/>
        </Style>
    </Window.Styles>

    <Grid Margin="25">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" CornerRadius="12" Margin="0,0,0,20">
            <Border.Background>
                <LinearGradientBrush StartPoint="0%,0%" EndPoint="100%,0%">
                    <GradientStop Color="#FFB6C1" Offset="0"/>
                    <GradientStop Color="#98D8C8" Offset="1"/>
                </LinearGradientBrush>
            </Border.Background>
            <Grid Margin="25,20">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="☁️" FontSize="32" Margin="0,0,15,0"/>
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="Google Drive - XML та HTML файли"
                                   FontSize="24" FontWeight="Bold" Foreground="White"
                                   Margin="0,0,0,5"/>
                        <TextBlock Text="Завантажуйте та зберігайте ваші файли"
                                   FontSize="13" Foreground="#FFFFFF" Opacity="0.9"/>
                    </StackPanel>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Files List -->
        <Border Grid.Row="1" Background="White" CornerRadius="12" Padding="20" Margin="0,0,0,15">
            <Border.Effect>
                <DropShadowEffect Color="#20000000" BlurRadius="15" OffsetX="0" OffsetY="2"/>
            </Border.Effect>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Header with refresh button -->
                <Grid Grid.Row="0" Margin="0,0,0,15">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="📁" FontSize="20" Margin="0,0,10,0"/>
                        <TextBlock Text="Ваші XML та HTML файли" FontSize="17" FontWeight="Bold"
                                   Foreground="#333333" VerticalAlignment="Center"/>
                    </StackPanel>

                    <Button HorizontalAlignment="Right" Command="{Binding RefreshCommand}"
                            Background="#A8E6CF">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="🔄" FontSize="14" Margin="0,0,6,0"/>
                            <TextBlock Text="Оновити" Foreground="White"/>
                        </StackPanel>
                    </Button>
                </Grid>

                <!-- File List -->
                <Border Grid.Row="1" BorderBrush="#E0E0E0" BorderThickness="2" CornerRadius="8">
                    <Grid>
                        <!-- Loading indicator -->
                        <StackPanel IsVisible="{Binding IsLoading}"
                                    VerticalAlignment="Center"
                                    HorizontalAlignment="Center">
                            <TextBlock Text="⏳" FontSize="48" TextAlignment="Center" Margin="0,0,0,15"/>
                            <TextBlock Text="Завантаження..." FontSize="16" Foreground="#999999"
                                       TextAlignment="Center"/>
                        </StackPanel>

                        <!-- Empty state -->
                        <StackPanel IsVisible="{Binding !IsLoading}"
                                    VerticalAlignment="Center"
                                    HorizontalAlignment="Center">
                            <StackPanel.IsVisible>
                                <MultiBinding Converter="{x:Static BoolConverters.And}">
                                    <Binding Path="!IsLoading"/>
                                    <Binding Path="Files.Count" Converter="{x:Static ObjectConverters.Equal}" ConverterParameter="0"/>
                                </MultiBinding>
                            </StackPanel.IsVisible>
                            <TextBlock Text="📭" FontSize="48" TextAlignment="Center" Margin="0,0,0,15"/>
                            <TextBlock Text="Файлів не знайдено" FontSize="16" Foreground="#999999"
                                       TextAlignment="Center" Margin="0,0,0,5"/>
                            <TextBlock Text="Завантажте XML або HTML файл на Google Drive" FontSize="13" Foreground="#BBBBBB"
                                       TextAlignment="Center"/>
                        </StackPanel>

                        <!-- Files List -->
                        <ListBox ItemsSource="{Binding Files}"
                                 SelectedItem="{Binding SelectedFile}"
                                 Background="Transparent"
                                 BorderThickness="0">
                            <ListBox.IsVisible>
                                <MultiBinding Converter="{x:Static BoolConverters.And}">
                                    <Binding Path="!IsLoading"/>
                                    <Binding Path="Files.Count" Converter="{x:Static ObjectConverters.NotEqual}" ConverterParameter="0"/>
                                </MultiBinding>
                            </ListBox.IsVisible>
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <Border Background="#FAFAFA" CornerRadius="6" Padding="15,10"
                                            BorderBrush="#FFD1DC" BorderThickness="1">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>

                                            <TextBlock Grid.Column="0" Text="📄" FontSize="24" Margin="0,0,15,0"
                                                       VerticalAlignment="Center"/>

                                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                                <TextBlock Text="{Binding DisplayName}" FontSize="14" FontWeight="SemiBold"
                                                           Foreground="#333333" Margin="0,0,0,3"/>
                                                <StackPanel Orientation="Horizontal">
                                                    <TextBlock Text="📅 " FontSize="11" Foreground="#999999"/>
                                                    <TextBlock Text="{Binding DisplayDate}" FontSize="11" Foreground="#999999"
                                                               Margin="0,0,15,0"/>
                                                    <TextBlock Text="💾 " FontSize="11" Foreground="#999999"/>
                                                    <TextBlock Text="{Binding DisplaySize}" FontSize="11" Foreground="#999999"/>
                                                </StackPanel>
                                            </StackPanel>

                                            <Border Grid.Column="2" Background="#E8F5E9" Padding="8,4" CornerRadius="4"
                                                    Margin="0,0,10,0" VerticalAlignment="Center">
                                                <TextBlock Text="✓ На Drive" FontSize="10" Foreground="#4CAF50"
                                                           FontWeight="SemiBold"/>
                                            </Border>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </Grid>
                </Border>
            </Grid>
        </Border>

        <!-- Status Bar -->
        <Border Grid.Row="2" Background="#F0F0F0" CornerRadius="8" Padding="15,10" Margin="0,0,0,15">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="ℹ️" FontSize="16" Margin="0,0,10,0"/>
                <TextBlock Text="{Binding StatusText}" FontSize="13" Foreground="#666666"
                           VerticalAlignment="Center"/>
            </StackPanel>
        </Border>

        <!-- Upload Format Selection -->
        <Border Grid.Row="3" Background="White" CornerRadius="8" Padding="15,10" Margin="0,0,0,10">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="📁 Формат:" FontSize="14" FontWeight="SemiBold"
                           Foreground="#666666" VerticalAlignment="Center" Margin="0,0,15,0"/>
                <ComboBox ItemsSource="{Binding UploadFormats}"
                          SelectedItem="{Binding SelectedUploadFormat}"
                          Width="120"
                          Background="#FFF9FB"
                          BorderBrush="#FFD1DC"
                          Foreground="#333333"/>
                <TextBlock Text="(для збереження на Drive)" FontSize="12"
                           Foreground="#999999" VerticalAlignment="Center" Margin="10,0,0,0"/>
            </StackPanel>
        </Border>

        <!-- Action Buttons -->
        <Grid Grid.Row="4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Download Button -->
            <Button Grid.Column="0" Command="{Binding DownloadFileCommand}"
                    HorizontalAlignment="Stretch">
                <Button.Background>
                    <LinearGradientBrush StartPoint="0%,0%" EndPoint="100%,0%">
                        <GradientStop Color="#A8E6CF" Offset="0"/>
                        <GradientStop Color="#98D8C8" Offset="1"/>
                    </LinearGradientBrush>
                </Button.Background>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                    <TextBlock Text="⬇️" FontSize="16" Margin="0,0,8,0"/>
                    <TextBlock Text="Завантажити з Drive" Foreground="White" FontWeight="Bold"/>
                </StackPanel>
            </Button>

            <!-- Upload Button -->
            <Button Grid.Column="1" Command="{Binding UploadCurrentFileCommand}"
                    HorizontalAlignment="Stretch">
                <Button.Background>
                    <LinearGradientBrush StartPoint="0%,0%" EndPoint="100%,0%">
                        <GradientStop Color="#FFD1DC" Offset="0"/>
                        <GradientStop Color="#FFB6C1" Offset="1"/>
                    </LinearGradientBrush>
                </Button.Background>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                    <TextBlock Text="⬆️" FontSize="16" Margin="0,0,8,0"/>
                    <TextBlock Text="Зберегти на Drive" Foreground="White" FontWeight="Bold"/>
                </StackPanel>
            </Button>

            <!-- Close Button -->
            <Button Grid.Column="2" Command="{Binding CloseCommand}"
                    Background="#FFC9D6"
                    HorizontalAlignment="Stretch">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                    <TextBlock Text="✖" FontSize="16" Margin="0,0,8,0"/>
                    <TextBlock Text="Закрити" Foreground="White" FontWeight="Bold"/>
                </StackPanel>
            </Button>
        </Grid>
    </Grid>
</Window>
